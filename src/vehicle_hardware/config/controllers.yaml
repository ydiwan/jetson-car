controller_manager:
  ros__parameters:
    update_rate: 50  # 50 Hz
    ackermann_steering_controller:
      type: ackermann_steering_controller/AckermannSteeringController
    joint_state_broadcaster:
      type: joint_state_broadcaster/JointStateBroadcaster

# Configuration for the Ackermann Controller
ackermann_steering_controller:
  ros__parameters:
    rear_wheels_names: ['rl_wheel_joint', 'rr_wheel_joint']
    front_wheels_names: ['fl_steering_joint', 'fr_steering_joint']
    # Kinematic parameters
    wheelbase: 0.148
    front_wheel_track: 0.133
    rear_wheel_track: 0.133
    front_wheels_radius: 0.034
    rear_wheels_radius: 0.034

    # Odometry Publishing settings
    publish_rate: 50.0
    odom_frame_id: odom
    base_frame_id: base_link
    enable_odom_tf: false
    
    # Input twist scaling and limits
    linear:
      x:
        has_velocity_limits: true
        max_velocity: 1.5   # m/s
        has_acceleration_limits: true
        max_acceleration: 2.0 # m/s^2
    angular:
      z:
        has_position_limits: true
        max_position: 0.785 # 45 degrees in rad